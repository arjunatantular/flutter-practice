class Chunk {

  Chunk();
  // When you come back: Write out how you want to do this.
// THe core idea is this should have a .fromJson that creates a List of BookData (use their .fromJson too).
// This .fromJson pulls from the previous request chunk, readChunk(int)
}